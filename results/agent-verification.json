{
  "addExpense": {
    "unresolved": [
      {
        "anchor": "screen.addExpense",
        "file": "Ledgerly/AddExpenseView.swift",
        "why": "No colour change in source \u2014 Section(\"New expense\") header still uses the default form-header foreground, so contrast failure stands. Fixer skipped it as non-mechanical, which is honest, but the defect is still live.",
        "fix": "Set an explicit header foreground meeting 4.5:1 against grouped-form background in light and dark, e.g. Section(header: Text(\"New expense\").foregroundStyle(Color(\"FormHeader\"))) with an asset-catalog colour tuned per appearance."
      }
    ],
    "regressions": [],
    "confirmed": [
      "add.title",
      "add.amount",
      "add.save",
      "add.cancel",
      "add.date"
    ]
  },
  "expenseDetail": {
    "unresolved": [
      {
        "anchor": "screen.expenseDetail",
        "file": "Ledgerly/ExpenseDetailView.swift",
        "why": "Contrast failure on the \"Details\" section header is untouched \u2014 line 9 is still a bare Section(\"Details\") with no foreground/background colour change anywhere in the file. Fixer correctly declared it skipped (mechanical: false), but the defect stands.",
        "fix": "Design picks a header foreground/background pair reaching >=4.5:1, then apply it to the Section header (e.g. .foregroundStyle(...) on a custom header label or a listRowBackground/tint change)."
      },
      {
        "anchor": "screen.expenseDetail",
        "file": "Ledgerly/ExpenseDetailView.swift",
        "why": "Note row Dynamic Type scaling is untouched \u2014 line 15 is still LabeledContent(\"Note\", value: expense.note) with no scalable text style or row-height allowance; the 52pt fixed cell height from the audit is unchanged.",
        "fix": "Design picks the replacement text style; then apply a scalable style (e.g. .font(.body) with .dynamicTypeSize range removed) and let the row grow (.fixedSize(horizontal: false, vertical: true) / remove height constraints)."
      }
    ],
    "regressions": [],
    "confirmed": [
      "detail.delete",
      "detail.flag"
    ]
  },
  "expenseList": {
    "unresolved": [
      {
        "anchor": "row.container",
        "file": "Ledgerly/ExpenseListView.swift",
        "why": "Contrast finding untouched. Line 58 still `.foregroundStyle(.secondary)` on the `.caption` category text; section header Text (line 16) carries no explicit foreground either, so it keeps the default secondary header treatment. Both contrast entries from the audit still stand. Fixer skipped deliberately (mechanical:false), but defect is live in code.",
        "fix": "Replace `.foregroundStyle(.secondary)` on the category caption with a colour asset measured at >=4.5:1 against the list background in both light and dark mode; same for the section header Text."
      }
    ],
    "regressions": [],
    "confirmed": [
      "expenses.add",
      "row.icon",
      "screen.expenseList"
    ]
  },
  "settings": {
    "unresolved": [
      {
        "anchor": "settings.currency",
        "file": "Ledgerly/SettingsView.swift",
        "why": "Not touched. Line 14 still has .foregroundStyle(Color(white: 0.72)) on the Picker, so the 'USD' text still fails contrast. Fixer skipped it deliberately (mechanical: false), but the defect stands in the shipped code.",
        "fix": "Remove the hard-coded Color(white: 0.72) so the Picker uses the semantic foreground colour, or substitute a designer-approved tone clearing 4.5:1 on the grouped-Form background."
      },
      {
        "anchor": "screen.settings",
        "file": "Ledgerly/SettingsView.swift",
        "why": "Not touched. Section(\"General\") and Section(\"About\") headers still use default styling; the near-miss contrast reported by the audit is unchanged.",
        "fix": "Raise section-header foreground contrast against the grouped-Form background with a designer-signed-off tone."
      }
    ],
    "regressions": [],
    "confirmed": [
      "settings.notifications",
      "settings.roundup",
      "settings.privacy",
      "settings.version",
      "card.container",
      "settings.limit.title"
    ]
  },
  "stats": {
    "unresolved": [],
    "regressions": [],
    "confirmed": [
      "stats.total",
      "card.action",
      "stats.largest.row",
      "stats.header.categories",
      "stats.header.largest"
    ]
  }
}
